from Buffer import Buffer
import Token

class Lexer:
    def Buffer.proximo():
        estado = 0
        ultimoFinal = -1
        Buffer.marcarInicio()

        while(True):
            c = Buffer.proximo()
            if(estado == 0):
                if(delimitador)
