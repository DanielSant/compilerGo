class Token:

    def variaveis(self, classe, valor):
        self.classe = classe
        self.valor = valor

    def Token(self, cla, val):
        classe = self.cla
        valor = self.val

    def Token(self, c):
        Token(self, c, null)